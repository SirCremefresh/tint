export const GeneralHelper: {
    port: number,
    localhost: string,
    api: string,
    prefixMeta: string,
    routeMeta: string
} = {
    port: 5000,
    localhost: "http://localhost:8080",
    api: "/api/",
    prefixMeta: "prefix",
    routeMeta: "routes"
};